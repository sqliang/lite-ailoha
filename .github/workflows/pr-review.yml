# TODO: 暂时停用自动 PR Review Workflow。
# 原因：当前项目处于快速迭代阶段，PR 频率高，自动触发会消耗 API token 且产生噪音。
# 计划在功能稳定后重新启用。取消注释即可恢复。
#
# name: Automatic PR Review
#
# on:
#   pull_request:
#     types: [opened, synchronize, reopened, ready_for_review]
#     branches-ignore:
#       - 'dependabot/**'
#
# concurrency:
#   group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
#   cancel-in-progress: true
#
# permissions:
#   contents: read
#   pull-requests: write
#   issues: write
#
# jobs:
#   review:
#     if: github.event.pull_request.draft == false && github.event.pull_request.head.repo.fork == false
#     runs-on: ubuntu-latest
#     timeout-minutes: 20
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#
#       - name: Run pr-review
#         uses: anthropics/claude-code-action@v1
#         with:
#           anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
#           prompt: "/pr-review ${{ github.event.pull_request.number }}"
#           claude_args: "--max-turns 12 --permission-mode bypassPermissions"
#         env:
#           GH_TOKEN: ${{ github.token }}
